import 'package:flutter/material.dart';

final milisec = Duration(milliseconds: 300);
String greet;
var putihh = Colors.white;
var putih = TextStyle(color: Colors.white);
final not = Align(
    alignment: Alignment.bottomRight,
    child: Text(
      "Maksimal 20 karakter",
      style: TextStyle(color: Colors.grey.shade500, fontSize: 12),
    ));
final nit = Align(
    alignment: Alignment.bottomRight,
    child: Text(
      "@ untuk email",
      style: TextStyle(color: Colors.grey.shade500, fontSize: 12),
    ));
final nut = Align(
    alignment: Alignment.bottomRight,
    child: Text(
      " ",
      style: TextStyle(color: Colors.grey.shade500, fontSize: 12),
    ));
